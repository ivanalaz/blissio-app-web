/**
 * Primarni jezik: srpski (Srbija).
 * Za skaliranje: uvedite next-intl ili sličan sloj i podelu stringova po locale,
 * ili App Router segment `app/[locale]/...` sa `generateStaticParams`.
 */
export const primaryLocale = "sr-RS" as const;
export const primaryHtmlLang = "sr" as const;
